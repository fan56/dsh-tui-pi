# MCP adapter (/mcp)

[@aiwayds/dsh-mcp-adapter](https://github.com/fan56/dsh-mcp-adapter) — optional companion plugin; folds every `mcp__*` tool schema into two constant meta-tools so prompt cost stays O(1) in servers/tools, and adds the `/mcp` command — server/tool tree, folding stats, and a per-server disable/enable latch:

<video src="https://github.com/fan56/dsh-tui-pi/raw/main/assets/demos/mcp.mp4" controls></video>

---

[← Back to README](../../README.md)
